# Google Gemini API Key (Required)
# Get free key at: https://aistudio.google.com/apikey
GOOGLE_API_KEY=your_api_key_here

# Optional: Anthropic API Key for comparison experiments
# ANTHROPIC_API_KEY=sk-ant-xxxxx

# NOTE: All configuration settings (model, hyperparameters, feature flags)
# are now defined in config.yaml (single source of truth).
# This .env file should ONLY contain secrets (API keys).
# See config.yaml for all configuration options.
